@section Styles
{
    <link rel="stylesheet" href="~/css/forum.css"/>    
}

@model ForumPostCreateViewModel

@{
    ViewData["Title"] = "Create Post";
}

<div class="py-5 container">
    <div class="bg-white p-5 animate__animated animate__fadeInUp rounded shadow-lg">
        <h2 class="text-dark fw-bold mb-4 border-bottom border-danger pb-3">📝 Create New Forum Post</h2>

        <form asp-action="Create" method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="All" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Title" class="form-label fw-semibold text-dark">Title</label>
                <input asp-for="Title" class="form-control shadow-sm" placeholder="Enter your post title" />
            </div>

            <div class="mb-3">
                <label asp-for="Content" class="form-label fw-semibold text-dark">Content</label>
                <textarea asp-for="Content" class="form-control shadow-sm" rows="6" placeholder="Write your post content..."></textarea>
            </div>

            <div class="mb-4">
                <label asp-for="TopicId" class="form-label fw-semibold text-dark">Topic</label>
                <select asp-for="TopicId" class="form-select shadow-sm" asp-items="Model.Topics">
                    <option value="">-- Select Topic --</option>
                </select>
            </div>

            <div class="form-group mb-4">
                <label class="form-label fw-semibold text-dark">Upload Images (Max 10)</label>
                <div id="dropZone" class="p-5 bg-light mb-3 position-relative rounded border border-2 border-dashed text-center" style="cursor:pointer;">
                    <p><i class="bi bi-cloud-arrow-up fs-1 text-muted"></i></p>
                    <p class="text-muted">Drag & drop images here, or click to select</p>
                    <input id="imageInput" asp-for="ImageFiles" type="file" class="form-control d-none" multiple accept="image/*" />
                </div>
                <div id="previewContainer" class="row"></div>
                <span asp-validation-for="ImageFiles" class="text-danger d-block mt-2"></span>
            </div>

            <div class="d-flex justify-content-between align-items-center mt-4 text-end">
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Cancel
                </a>

                <button type="submit" class="btn fw-bold shadow" style="background-color: #d81324; color: white;">
                    <i class="bi bi-plus-circle"></i> Create Post
                </button>
            </div>

        </form>
    </div>
</div>

@section Scripts
{
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/Forum/create.js"></script>
}
